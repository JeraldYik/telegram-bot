[project]
name = "mcc-rewards-bot"
version = "0.1.0"
description = "Telegram MCC credit card rewards lookup bot"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
